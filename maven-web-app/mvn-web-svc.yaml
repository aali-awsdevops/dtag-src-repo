apiVersion: v1
kind: Service
metadata:
  name: maven-web-app
  namespace: dtag-dev
spec:
  type: ClusterIP
  selector:
    app: mavenwebapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
